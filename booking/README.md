# Запись на съёмку — как это работает и как настроить

Клиент открывает страницу, выбирает **вид съёмки → пакет → дату → время**, оставляет контакты. Дальше всё происходит само:

- съёмка появляется в Google Календаре `bjelobrkovic.ph@gmail.com`, а занятые часы больше не предлагаются другим клиентам;
- свободное время считается по трём календарям сразу: `bjelobrkovic.ph`, `elenabjelobrkovic` и `nowcreativespace305` — если занято хоть в одном, час не предлагается;
- вам приходит уведомление **в Telegram** (с кнопкой «написать клиенту в WhatsApp») и **на почту**;
- **за 2 дня до съёмки** вам приходит напоминание с контактами клиента — чтобы вы ему написали.

Клиенту скрипт ничего не отправляет: на экране после записи он видит, что вы свяжетесь с ним лично.

Файлы:

| Файл | Что это |
|---|---|
| `index.html`, `style.css`, `app.js` | сама страница — трогать не нужно |
| **`config.js`** | **все ваши тексты, пакеты, цены и часы работы** |
| `apps-script/Code.gs` | скрипт для вашего Google-аккаунта (календарь, почта, Telegram, напоминания) |

---

## 1. Подключение почты и календаря (10 минут, делается один раз)

1. Откройте <https://script.google.com> под аккаунтом **bjelobrkovic.ph@gmail.com** — именно в его календаре будут появляться съёмки. Нажмите **New project**.
2. Удалите содержимое файла `Code.gs` в редакторе и вставьте туда всё из `apps-script/Code.gs`.
3. Вверху файла, в блоке «НАСТРОЙКИ», при необходимости поменяйте:
   - `NOTIFY_EMAIL` — если письма должны приходить на другой адрес (пусто = на почту этого же аккаунта);
   - `CALENDAR_ID` — если съёмки должны идти в отдельный календарь, а не в основной;
   - `SEND_CLIENT_CONFIRMATION` и `INVITE_CLIENT` — по умолчанию выключены: клиенту скрипт не пишет (см. раздел 4);
   - `BUSY_CALENDAR_IDS` — чужие календари, которые учитываются как занятость (см. раздел 2).
4. Сохраните (значок дискеты).
5. Нажмите **Deploy → New deployment**, тип **Web app**, и укажите:
   - **Execute as:** Me (ваш аккаунт)
   - **Who has access:** **Anyone** — иначе клиенты не смогут отправить заявку.
6. Нажмите **Deploy**, разрешите доступ к Календарю и Gmail (Google покажет предупреждение «Google hasn't verified this app» → *Advanced* → *Go to project*).
7. Скопируйте **Web app URL** (вида `https://script.google.com/macros/s/…/exec`) и вставьте его в `config.js`:

```js
endpoint: 'https://script.google.com/macros/s/ВАШ_АДРЕС/exec',
```

**Проверка:** в редакторе скрипта выберите функцию `testBooking` и нажмите ▶ «Run». На почту придёт тестовое письмо, а в календаре на завтра появится тестовая съёмка — её потом удалите.

> Пока `endpoint` пустой, страница всё равно работает: при отправке заявки у клиента открывается готовое письмо с заявкой на вашу почту. Просто ничего не происходит автоматически.

**Важно:** после любого изменения `Code.gs` нужно снова сделать **Deploy → Manage deployments → ✏️ → Version: New version → Deploy**, иначе изменения не подхватятся.

---

## 2. Чужие календари: чтобы не предлагать занятое время

Съёмки записываются в календарь `bjelobrkovic.ph@gmail.com`, но занятость считается по трём календарям сразу — к нему добавлены `elenabjelobrkovic@gmail.com` и `nowcreativespace305@gmail.com`. Если в любом из них на это время что-то стоит, клиенту такой час не предложится.

Чтобы скрипт мог их видеть, каждый из двух календарей нужно **один раз расшарить**. Для каждого аккаунта (`elenabjelobrkovic@gmail.com`, потом `nowcreativespace305@gmail.com`):

1. Войдите в <https://calendar.google.com> под этим аккаунтом.
2. Слева в списке «Мои календари» наведите на свой основной календарь → **⋮** → **Настройки и общий доступ**.
3. Раздел **Открыть доступ отдельным пользователям** → **+ Добавить пользователей**.
4. Впишите **bjelobrkovic.ph@gmail.com**, права — **Просмотр всех сведений о мероприятиях**, нажмите **Отправить**.
5. Зайдите в почту `bjelobrkovic.ph@gmail.com` и примите приглашение из письма.

**Проверка:** в редакторе скрипта выберите функцию `testCalendars`, нажмите ▶ Run и откройте **View → Logs**. Там будет список: куда пишутся съёмки и какие календари скрипт видит. Строка `NIJE DOSTUPAN` означает, что этот календарь ещё не расшарен — вернитесь к пункту 4.

Список календарей задаётся в `Code.gs`:

```js
var BUSY_CALENDAR_IDS = [
  'elenabjelobrkovic@gmail.com',
  'nowcreativespace305@gmail.com'
];
```

Добавить ещё один — впишите его адрес в этот список (через запятую) и расшарьте так же. Календарь, куда идёт запись, учитывается всегда, отдельно писать его сюда не нужно.

---

## 3. Уведомления вам в Telegram (5 минут, бесплатно)

1. В Telegram найдите **@BotFather**, отправьте `/newbot` и придумайте имя. Он пришлёт **токен** вида `1234567890:AAH...`.
2. Вставьте токен в `Code.gs`, строка `TELEGRAM_BOT_TOKEN`, и сохраните.
3. Найдите своего бота в поиске Telegram, откройте чат и **напишите ему любое сообщение** (иначе он не сможет вам писать первым).
4. В редакторе скрипта выберите функцию `showTelegramChatId` и нажмите ▶ «Run». Откройте **View → Logs** — там будет строка `chat_id: 123456789`.
5. Вставьте это число в `TELEGRAM_CHAT_ID`, сохраните и передеплойте (**Deploy → Manage deployments → ✏️ → New version → Deploy**).
6. Проверьте: запустите функцию `testTelegram` — в чат придёт «Telegram je povezan».

Теперь каждая новая запись приходит в чат с датой, пакетом и контактами клиента, а кнопка **«Napiši na WhatsApp»** открывает переписку с клиентом в один клик.

> Хотите получать заявки в общий чат или канал — добавьте туда бота, напишите там сообщение и снова запустите `showTelegramChatId`: у групп `chat_id` отрицательный, вставляйте его целиком, вместе с минусом.

---

## 4. Напоминание вам за 2 дня до съёмки

Скрипт раз в день просматривает календарь и присылает вам напоминание о съёмках, до которых осталось 2 дня: вид съёмки, пакет, время и контакты клиента, плюс кнопка «Napiši na WhatsApp» — чтобы написать клиенту в один клик. В Telegram приходит по сообщению на каждую съёмку, на почту — одним письмом списком.

Чтобы включить, **один раз** запустите в редакторе функцию `installReminderTrigger` (▶ Run) — она поставит ежедневный запуск.

Настройки в `Code.gs`:
- `REMIND_ME_BEFORE` — `false` полностью выключает напоминания;
- `REMINDER_DAYS_BEFORE` — за сколько дней напоминать (сейчас 2);
- `REMINDER_HOUR` — в котором часу присылать (сейчас 10).

Если поменяете `REMINDER_HOUR`, запустите `installReminderTrigger` ещё раз — старый триггер заменится новым.

Напоминание приходит только по записям, сделанным через сайт, и только один раз: после отправки съёмка помечается. Если запись отменилась — удалите событие из календаря, напоминание не придёт.

### Если захотите писать клиенту автоматически

Клиенту сейчас ничего не отправляется. Письмо-подтверждение сразу после записи уже написано и включается одной строкой в `Code.gs`:

```js
var SEND_CLIENT_CONFIRMATION = true;
```

Там же `INVITE_CLIENT = true` добавит клиента гостем в событие календаря — тогда Google сам пришлёт ему приглашение и напоминание.

---

## 5. Что вы правите в `config.js`

Открывайте файл в любом текстовом редакторе. Меняйте только текст между кавычками и числа.

**Ваши контакты** — блок `photographer`. Клиенту внизу страницы показываются только `studio`, `website` и `instagram` (пустое поле просто не выводится). Телефон и почта на странице не публикуются. Отдельно стоит `fallbackEmail` — он не отображается, на этот адрес уходит письмо с заявкой, пока не подключён Apps Script.

**Часы работы студии** — блок `schedule`:

```js
schedule: { days: [1, 2, 3, 4, 5], start: '10:00', end: '14:00', stepMin: 30 },
```

`days`: 0 — воскресенье, 1 — понедельник, … 6 — суббота. `stepMin` — с каким шагом предлагать начало съёмки.
У съёмок «на выезде» (`proslave`, `krstenje`) свой блок `schedule` внутри вида съёмки — он перебивает общий, поэтому там доступны и выходные, и вечернее время.

**Пакеты.** У каждого пакета:

```js
{
  id: 'baby-start',          // служебное имя, латиницей, менять не нужно
  name: 'Baby Start',        // как видит клиент
  price: 150,                // число; '' — цена не показывается
  priceFrom: true,           // необязательно: выводит «od 150 €»
  durationMin: 120,          // сколько времени занимает съёмка — влияет на свободные часы
  summary: 'Beba + roditelji | do 2 sata | 20 fotografija',
  includes: ['…', '…'],      // список «U paket je uključeno»
  notes: ['Torta nije uključena u paket.']   // необязательно, розовая плашка
}
```

Чтобы **добавить** пакет — скопируйте такой блок целиком (вместе с фигурными скобками и запятой) и поменяйте текст.
Чтобы **убрать** — удалите блок целиком.

**Прочие параметры:**
- `bufferMin` — перерыв между двумя съёмками (те же 15 минут стоят и в `Code.gs`, меняйте вместе);
- `bookAheadDays` — насколько далеко вперёд можно записаться;
- `minNoticeHours` — за сколько часов минимум принимается запись.

---

## 6. Как открыть страницу

Локально: открыть `booking/index.html` в браузере (или запустить `python3 -m http.server` в корне репозитория и зайти на `http://localhost:8000/booking/`).

Онлайн через GitHub Pages: в репозитории **Settings → Pages → Source: Deploy from a branch**, ветка с этим кодом, папка `/ (root)`. Через минуту страница будет доступна по адресу `https://<логин>.github.io/<репозиторий>/booking/` — эту ссылку можно ставить в шапку профиля.

---

## 7. Что уже заполнено

Данные пакетов взяты со скриншотов вашего сайта: Newborn, Trudnice, Bebe do godine dana, Prvi rođendan, Djeca, Porodično, Proslave, Krštenje — с ценами, длительностью, составом и примечаниями (торт, аллергии, сроки бронирования, скидка 10 %, транспорт за пределы Бара).

Внизу страницы и на экране «спасибо» клиент видит `Bar, Crna Gora` и ссылку на `elenabjelobrkovic.com`. Телефон и почта не публикуются.

Почта `bjelobrkovic.ph@gmail.com` осталась в двух местах, невидимых клиенту: `NOTIFY_EMAIL` в `Code.gs` (куда приходят заявки) и `fallbackEmail` в `config.js` (адрес письма, пока не подключён Apps Script).

Не заполнено только `photographer.instagram` — впишете ссылку, и рядом с адресом сайта появится ссылка на профиль.
